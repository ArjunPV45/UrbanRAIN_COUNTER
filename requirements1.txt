# Core Flask dependencies
Flask==2.3.3
Flask-SocketIO==5.3.6

# Computer Vision and Image Processing
opencv-python==4.8.1.78
numpy==1.24.3

# Async and Threading
eventlet==0.33.3

# GStreamer Python bindings (system package - install via apt)
# sudo apt-get install python3-gi python3-gi-cairo gir1.2-gtk-3.0
# sudo apt-get install gstreamer1.0-tools gstreamer1.0-plugins-good gstreamer1.0-plugins-bad gstreamer1.0-plugins-ugly

# Hailo AI dependencies (specific to Hailo hardware)
# These should be installed according to Hailo's installation guide
# hailo-platform-api
# hailo-apps-infra

# System dependencies that need to be installed via apt:
# sudo apt-get update
# sudo apt-get install python3-dev
# sudo apt-get install libgstreamer1.0-dev libgstreamer-plugins-base1.0-dev
# sudo apt-get install libgirepository1.0-dev
# sudo apt-get install pkg-config
